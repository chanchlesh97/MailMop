{
  "name": "mailmop",
  "version": "0.1.0",
  "description": "MailMop — safely bulk-clean Gmail from the browser (MVP skeleton)",
  "scripts": {
    "lint": "eslint . --ext .js,.html",
    "start": "echo 'Load the extension in Chrome via chrome://extensions (Developer mode -> Load unpacked)'",
    "build": "webpack --config webpack.config.js --mode=development",
    "build:prod": "NODE_ENV=production webpack --config webpack.config.js --mode=production",
    "watch": "webpack --watch --config webpack.config.js"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^13.0.1",
    "eslint": "^8.0.0",
    "webpack": "^5.102.1",
    "webpack-cli": "^6.0.1"
  }
}
